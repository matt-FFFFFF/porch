---
name: "Directory Processor"
description: "Process each directory in series"
commands:
  - type: "foreach"
    name: "Process Directories"
    itemProvider: "list-directories"
    mode: "serial"
    cwd: "/Users/matt/code/avmtool/testdata"
    commands:
      - type: "serial"
        name: "Directory Commands"
        commands:
          - type: "command"
            command: "oscommand"
            name: "List Files"
            executable: "echo"
            args:
              - "$ITEM"
